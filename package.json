{
  "name": "icsProxy",
  "version": "0.0.1",
  "description": "An ics proxy to add basic authentication when services doesn't allow it",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Guillaume Wuip <guillaume@wuips.com> (http://guillaume.wuips.com)",
  "license": "wtfpl",
  "dependencies": {
    "querystring": "^0.2.0",
    "request": "^2.61.0"
  }
}
